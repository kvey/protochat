'use strict'

angular.module('protochatApp')
  .controller 'MainCtrl', ['$scope', '$routeParams', '$rootScope', ($scope, $routeParams, $rootScope) ->

    # track conversations user is actively participating in
    # display them by name and click to switch to them

    #get the current conversation hash and active a convo for it

  ]

